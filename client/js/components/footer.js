/** @jsx React.DOM */
var React = require('react');

var Footer =
    React.createClass({
        render: function() {
            return (
                <footer>
                	tribble.
                </footer>
            );
        }
    });

module.exports = Footer;