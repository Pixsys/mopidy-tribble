/** @jsx React.DOM */
var React = require('react');

var Footer =
    React.createClass({
        render: function() {
            return (
                <footer>
                	tribble it.
                </footer>
            );
        }
    });

module.exports = Footer;